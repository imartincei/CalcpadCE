# Calcpad.Web Security Audit

Findings from a security audit of `Calcpad.Web/backend` (ASP.NET Core server), `Calcpad.Web/frontend/calcpad-desktop` (Tauri v2 shell + sidecar), and `Calcpad.Web/frontend/vscode-calcpad` (VS Code extension). `calcpad-web` and `calcpad-frontend` were not audited in this pass and remain open.

> **Snapshot note (2026-08-12, branch `ui-updates`):** Line numbers are current as of this branch. Findings marked **[verified]** were reproduced directly — bypass executed, archive enumerated, or call chain traced end to end. Findings marked **[reported]** carry file:line evidence from the discovery sweep but were not independently re-derived; spot-check before acting.
>
> **Verification pass (2026-08-13, HEAD `d3220f2`):** Every claim marked done or fixed above was re-derived against the code, file by file — Pass 1, Pass 2 (2/4/5), Pass 3, and all six Pass 5 items hold as written, and nothing in *What holds up* regressed. Four entries were stale and are corrected in place: `BrowserPath` is fixed rather than open, the Pass 5.1 CORS-header description named a mechanism the code does not use, Swagger sits outside the token middleware's `/api` prefix, and the local `bin/logs/` deletion never happened. One functional regression from Pass 5.3 was found and fixed (see that item).

## Threat model

The server binds to loopback only and this is enforced twice (see *What holds up* below), so the adversary is not a remote attacker. It is:

1. A **malicious `.cpd`/`.cpdz` worksheet** the user opens — worksheets legitimately carry author HTML/JS, so this is untrusted input by design.
2. **Any other local process**, which reaches the API with no `Origin` header and therefore no CORS check.
3. **Any page served from loopback**, which `IsAllowedOrigin` admits on any port.

Findings are rated against those three, not against internet exposure.

## Critical

-   ~~**VS Code preview webviews execute arbitrary JS from any opened worksheet.**~~ **CONTAINED — see Pass 1 below (2026-08-13).** Worksheet script still runs, by design, but now inside a sandboxed `<iframe srcdoc>` on an opaque origin: no `acquireVsCodeApi`, no storage, no reach into the shell, and `postMessage` to a fixed relay as the only channel out. Network egress is unchanged and remains accepted. Original finding kept below for the record.

-   **VS Code preview webviews execute arbitrary JS from any opened worksheet.** [extension.ts:495-506](../frontend/vscode-calcpad/src/extension.ts#L495-L506) — `sanitizeServerHtml` only escapes stray `<`, which is exactly what its own comment at [:492-494](../frontend/vscode-calcpad/src/extension.ts#L492-L494) says it does; the misnomer is the function name and the call-site comment at [:983](../frontend/vscode-calcpad/src/extension.ts#L983). Running its regex verbatim, 5 of 6 payloads pass through unchanged: `<script>`, `<img onerror>`, `<svg onload>`, `<a href="javascript:">`, `<iframe>`. Only `<h` is modified. Output is assigned to `panel.webview.html` on a panel created with `enableScripts: true` and no `localResourceRoots` ([:1518-1521](../frontend/vscode-calcpad/src/extension.ts#L1518-L1521)); `grep -c "Content-Security-Policy" src/extension.ts` returns **0**. Same shape in the `.cpdz` custom editor ([calcpadCompiledEditorProvider.ts:115-120, :144](../frontend/vscode-calcpad/src/calcpadCompiledEditorProvider.ts#L115-L120)). **[verified]**

    No user action is needed beyond opening the file: `autoInputMode` defaults to `true` ([extension.ts:208](../frontend/vscode-calcpad/src/extension.ts#L208)), so a `.cpd` containing `#UI` auto-opens the form webview. A `.cpdz` needs one click — it is bound at `"priority": "default"` in [package.json:31-42](../frontend/vscode-calcpad/package.json#L31-L42).

    Impact is confined to the webview — full network egress and exfiltration of everything rendered (including server-side `#include`d files), plus in-editor phishing. It does **not** reach the extension host: message handlers are a closed switch, `enableCommandUris` is never set, and the backend rejects `vscode-webview://` origins.

    Note the tension before fixing: author HTML/JS in worksheets is a Calcpad *feature* (`Examples/Engineering/Fractals/Koch Snowflake Animated.cpd` ships a jQuery `<script>` tag). Stripping scripts breaks the product. The fix is containment, not filtering.

## High

-   ~~**Arbitrary executable launch via the PDF endpoint (backend).**~~ **[fixed]** — `BrowserPath` is gone from [PdfGenerateRequest.cs](../backend/Models/Pdf/PdfGenerateRequest.cs), which now carries only `Html` and `Options`; [CalcpadController.cs:283-292](../backend/Controllers/CalcpadController.cs#L283-L292) forwards only those two, and `GeneratePdfAsync` never receives a path. The executable is resolved server-side and only there, by `ResolveBrowserPathAsync` ([PdfGeneratorService.cs:350-390](../backend/Services/PdfGeneratorService.cs#L350-L390)) from `appsettings`'s `BrowserPath` or the `BROWSER_PATH` environment variable. `API_SCHEMA.md` states plainly that the browser is never taken from the request. Landed in commit `962875d`; this entry said "still open" until the 2026-08-13 verification pass caught up with it. Original finding kept below for the record.

-   **Arbitrary executable launch via the PDF endpoint (backend).** `browserPath` is accepted on the request body ([PdfGenerateRequest.cs:8](../backend/Models/Pdf/PdfGenerateRequest.cs#L8)), passed through [CalcpadController.cs:292](../backend/Controllers/CalcpadController.cs#L292), returned as the first-choice path at [PdfGeneratorService.cs:351-358](../backend/Services/PdfGeneratorService.cs#L351-L358), and reaches `Puppeteer.LaunchAsync(ExecutablePath = …)` at [:298-303](../backend/Services/PdfGeneratorService.cs#L298-L303). The process spawns before Puppeteer determines it is not Chromium, so the payload runs regardless of launch success. UNC paths work on Windows. **[verified]**

    **No client anywhere in the repo set this field** — only `API_SCHEMA.md` mentioned it; the `appsettings`/`BROWSER_PATH` resolution below it is what was actually used, which is why deleting the property was a zero-breakage fix. Predated this branch (commit `b62416b`).

-   **Tauri webview holds whole-disk read.** [capabilities/default.json:65, :84, :103](../frontend/calcpad-desktop/src-tauri/capabilities/default.json#L65) — `{ "path": "**" }` on `fs:allow-read-text-file`, `fs:allow-read-file`, `fs:allow-read-dir`. The four scoped entries beside each (`$APPDATA/**`, `$APPCONFIG/**`, `$APPLOG/**`, `$HOME/**`) are fully subsumed, and `fs:deny-default` is never included, so nothing is denied — including the WebView2 data folder that `fs:deny-default` normally protects. The likely motivation is the `/etc/os-release` read at [tauri-bridge.ts:938](../frontend/calcpad-web/src/services/tauri-bridge.ts#L938), which sits outside every named base directory. **[verified]**

    **[fixed]** — see Pass 2.2. `**` is gone from all three read scopes, `fs:deny-default` is included, and `/etc/*-release` is scoped narrowly so `detectLinuxDistro` still works. Out-of-scope documents keep opening through the fs plugin's runtime scope, carried across restarts by `tauri-plugin-persisted-scope`.

-   **Tauri CSP cannot stop an injection.** [tauri.conf.json:23](../frontend/calcpad-desktop/src-tauri/tauri.conf.json#L23) — `script-src 'self' 'unsafe-inline' 'unsafe-eval' https:` and `connect-src 'self' ipc: http://127.0.0.1:* https:`. `dangerousDisableAssetCspModification: ["script-src", "style-src"]` at [:24](../frontend/calcpad-desktop/src-tauri/tauri.conf.json#L24) is what keeps `'unsafe-inline'` effective — Tauri's nonce injection would otherwise neutralise it per the CSP spec. Bare `https:` makes any HTTPS host both a script origin and an exfiltration sink. **[verified]**

    No live injection sink exists today (Vue escapes by default; `innerHTML` appears only with constant strings). This is the amplifier: it is what would turn any future injection into whole-disk read plus exfiltration with no CSP step in between.

-   **Published VSIX archives ship developer server logs.** All three built archives contain them:

    | Archive | Contents |
    | --- | --- |
    | `vscode-calcpad-0.3.0.vsix` | 2 log files, `.env.example`, `bin/appsettings.Development.json` |
    | `vscode-calcpad-0.4.0.vsix` | 4 log files, same |
    | `vscode-calcpad-0.4.2.vsix` | 4 log files, same |

    Logs leak worksheet source under `Content preview:` and local source-tree paths. `bin/` **is** gitignored ([.gitignore:32](../../.gitignore#L32)) so these are not committed — but `.vscodeignore` re-includes it with `!bin/**` and has no `bin/logs/**` exclusion, so every package build sweeps them in. **[verified]**

    **[fixed]** — `!bin/**` removed (vsce applies every negation *after* all ignores, so a re-include silently overrides any later exclusion; `bin/` simply isn't ignored now) and `bin/logs/**`, `bin/cache/**`, `bin/**/*.log`, `bin/.calcpad-server.lock`, `bin/appsettings.Development.json`, `.env`, `.env.*` excluded; the catch-all `*.log` widened to `**/*.log`. `appsettings.Development.json` is additionally cut off at the source with `CopyToPublishDirectory=Never` ([Calcpad.Server.csproj](../backend/Calcpad.Server.csproj)), so it no longer reaches any bundle that mirrors the publish tree. `vsce ls` verified clean. 0.3.0 / 0.4.0 / 0.4.2 still need republishing if they reached the Marketplace.

-   **Downloaded runtimes and DLLs are executed without integrity verification.** [dotnetRuntimeManager.ts:141-173](../frontend/vscode-calcpad/src/dotnetRuntimeManager.ts#L141-L173) downloads, extracts, and later executes the .NET runtime with no checksum and no signature check; Microsoft's release metadata carries a `hash` field the schema at [:263-274](../frontend/vscode-calcpad/src/dotnetRuntimeManager.ts#L263-L274) never reads. The URL is taken from fetched JSON with no host allowlist ([:297, :303](../frontend/vscode-calcpad/src/dotnetRuntimeManager.ts#L297)). Same pattern for `.nupkg` fetches at [calcpadServerManager.ts:229-242](../frontend/vscode-calcpad/src/calcpadServerManager.ts#L229-L242) and the native `libSkiaSharp` at [:352-368](../frontend/vscode-calcpad/src/calcpadServerManager.ts#L352-L368), which is `chmod 0o755` on POSIX. Versions are pinned; content is not. **[reported]**

    **[fixed]** — every download now goes through [downloadVerification.ts](../frontend/vscode-calcpad/src/downloadVerification.ts): HTTPS-plus-host-allowlist assertion on the pre-request URL *and* on the post-redirect `response.url`, then a constant-time SHA512 comparison against the digest published in the feed metadata, all in memory before anything reaches disk. Missing or non-SHA512 metadata is fatal rather than a silent skip. See Pass 3 below for the per-source details.

## Medium

-   **`.cpdz` decompression bomb (backend).** [CpdzCodec.cs:84-93](../backend/Services/CpdzCodec.cs#L84-L93) inflates into an unbounded `MemoryStream`, reachable from `/api/calcpad/cpdz/decode` with attacker-chosen base64. No `MaxRequestBodySize` is configured anywhere, so Kestrel's 30 MB default applies — at deflate ratios that is tens of GB into memory. New on this branch. **[verified]**

-   **Second, stronger arbitrary-file-read primitive (backend).** The `#include` reader at [CalcpadService.cs:29](../backend/Services/CalcpadService.cs#L29) is unconfined by design and documented as such. `/api/calcpad/portable/package` is now a second one: [PortablePackage.cs:463-478](../backend/Services/PortablePackage.cs#L463-L478) honours absolute paths, `..`, and expands environment variables, then [:140](../backend/Services/PortablePackage.cs#L140) `File.ReadAllBytes` returns **raw bytes** in the ZIP — binary files, not just text rendered into HTML. New on this branch. Needs a decision on whether this stays inside the accepted threat model. **[verified]**

-   **`shell:allow-open` reopens the scope `opener` closes.** [capabilities/default.json:20](../frontend/calcpad-desktop/src-tauri/capabilities/default.json#L20) grants it while `opener:allow-open-url` at [:32](../frontend/calcpad-desktop/src-tauri/capabilities/default.json#L32) is scoped to a single URL prefix. With no `plugins.shell` config the default validator permits any `http(s)://`, `mailto:`, `tel:`. No JS imports the shell plugin — an unused grant, so removal is free. **[verified — grant present; unused-ness reported]**

    **[fixed]** — see Pass 2.4. The grant, the plugin registration, and both the Rust and npm dependencies are gone.

-   **`open_path_native` bypasses the opener scope on Linux.** [lib.rs:372-404](../frontend/calcpad-desktop/src-tauri/src/lib.rs#L372-L404) shells to `xdg-open` with no scope validation, called unconditionally on Linux from [tauri-bridge.ts:693-699](../frontend/calcpad-web/src/services/tauri-bridge.ts#L693-L699). The Windows/macOS branch goes through `opener::open_path`, which does enforce scope. Not command injection — `Command::new` + `cmd.arg` passes through `execve` — but the allow-list at [capabilities/default.json:21-30](../frontend/calcpad-desktop/src-tauri/capabilities/default.json#L21-L30) is simply not applied on the primary target platform. **[reported]**

    **[fixed]** — see Pass 5.2. `open_path_native` now canonicalizes the path and requires it under the app's data / config / log dirs, mirroring the narrowed capability scope.

    **[reverted 2026-08-23]** — see the Pass 5.3 amendment. The containment check is gone along with the narrowed scope it mirrored; the leading-`-` rejection stays.

-   **`$HOME` write plus `$HOME` open-path is a write-then-execute chain.** `fs:allow-write-file` → `$HOME/**` combined with `opener:allow-open-path` → `$HOME/**` lets webview script write `x.hta` (or `.js`, `.bat`, `.cmd`, `.scr`) and then hand it to `ShellExecute`. `$RESOURCE/**` in the open-path list separately allows launching the sidecar directly, outside `spawn_sidecar`'s managed lifecycle. **[reported]**

    **[fixed]** — see Pass 5.3. Both `$HOME/**` and `$RESOURCE/**` are gone from `opener:allow-open-path`. The write half stands (`fs:allow-write-file` still reaches `$HOME`), but there is no longer an execute half to chain it to.

    **[reverted 2026-08-23 — accepted risk]** — see the Pass 5.3 amendment. `opener:allow-open-path` is now `**`, so this finding stands again and is wider than originally reported: not just `$HOME`, and not just files the webview wrote. The `$RESOURCE/**` `deny` that remains is a lifecycle guard, not a mitigation for this — `**` lets `open_path` reach `cmd.exe` and every other binary on the machine regardless. Accepted because reaching `open_path` requires main-webview script execution, for which no route exists today; see the amendment for the full reasoning and for the two weaknesses that argument leans on.

-   **Local server is unauthenticated.** [baseServerManager.ts:195, :289-290](../frontend/vscode-calcpad/src/baseServerManager.ts#L195) starts it with no token; the port is published in plaintext at `bin/.calcpad-server.lock`, and on the Tauri side in `std::env::temp_dir()` ([lib.rs:471-478](../frontend/calcpad-desktop/src-tauri/src/lib.rs#L471-L478)), mode 1777 on Linux. `CALCPAD_DETACHED=1` plus `detached`/`unref` makes it outlive VS Code. Any local process gets arbitrary-file-read-as-the-user via `#include`. The backend documents this exact risk at [CalcpadApiService.cs:63-70](../backend/Services/CalcpadApiService.cs#L63-L70). **[reported]**

    **[fixed]** — see Pass 5.1. Every `/api` route now requires a per-launch `X-Calcpad-Token`, generated by whichever host spawns the server and passed through the environment. The VS Code lock file publishes it for peer windows and is written mode `0600`; the Tauri port file is chmod'd before its URL is written. A launch with no `CALCPAD_API_TOKEN` — development only — is still open, and now says so in the log.

-   **`#include` resolution has no workspace containment.** [calcpadLocationResolver.ts:59-68](../frontend/vscode-calcpad/src/calcpadLocationResolver.ts#L59-L68) resolves relative paths, then falls through to an explicit absolute-path branch, with no check that the result stays under the workspace root. `expandEnvVars` at [:9-14](../frontend/vscode-calcpad/src/calcpadLocationResolver.ts#L9-L14) expands `%VAR%`/`$VAR` against the extension host's environment first, so `#include %USERPROFILE%\.aws\credentials` resolves. This is the file-read half of the Critical finding's exfiltration chain. **[reported]**

-   **Preview `srcdoc` frames inherit the host CSP (desktop).** Frames at [App.vue:415-423, :467-475](../frontend/calcpad-web/src/App.vue#L415-L423) are correctly `sandbox="allow-scripts"` without `allow-same-origin`, but `srcdoc` documents inherit the parent's CSP — so worksheet script gets `connect-src … https:` and can exfiltrate the rendered calculation. The opaque origin correctly blocks IPC and the API rejects `Origin: null`; only network egress is open. **[reported]**

    Correction (2026-08-13): tightening the host CSP does **not** fix this as a side effect — it breaks the preview, because the frame inherits whatever the host declares and worksheet script legitimately needs `'unsafe-inline'` and bare `https:`. The inheritance is the thing to remove, not the permissiveness; see Pass 2 item 3, Stage B.

-   **Zip extraction without traversal protection.** [dotnetRuntimeManager.ts:167-170](../frontend/vscode-calcpad/src/dotnetRuntimeManager.ts#L167-L170) uses PowerShell `Expand-Archive`, which does not reliably reject `../` entries. Only exploitable in combination with the missing download verification above. The sibling paths already use `ZipFile::ExtractToDirectory` and `unzip`, both of which do validate. **[reported]**

    **[fixed]** — the runtime `.zip` path now calls the shared `extractZipToDir`, so all three extraction sites (runtime archive, managed `.nupkg`, native `.nupkg`) go through `ZipFile::ExtractToDirectory` on Windows and `unzip` elsewhere. `Expand-Archive` no longer appears anywhere in the extension. The `.tar.gz` branch still uses `tar`, which refuses `..` members on both GNU tar and bsdtar.

## Low

-   `get_env` ([lib.rs:351-354](../frontend/calcpad-desktop/src-tauri/src/lib.rs#L351-L354)) returns any environment variable to the webview and is reachable from document-derived strings via `expandEnvVars`; a worksheet containing `$AWS_SECRET_ACCESS_KEY` in an include path leaks it into an error message. **[fixed]** — allow-listed to `HOME`, `USERPROFILE`, `APPDATA`, `XDG_*`, `CALCPAD_*` minus `CALCPAD_API_TOKEN` (Pass 5.4).
-   Updater config is a half-state: `"active": false` at [tauri.conf.json:75-81](../frontend/calcpad-desktop/src-tauri/tauri.conf.json#L75-L81) is a **Tauri v1** key and is silently discarded in v2. Inert today (`endpoints: []`), but the plugin is registered and `updater:default` is granted, so a maintainer adding endpoints later inherits an exposed `download_and_install`. **[fixed]** — plugin, capability, config block, and both dependencies removed (Pass 5.5).
-   `loadPreset` joins an unvalidated name ([calcpadSettings.ts:263-266](../frontend/vscode-calcpad/src/calcpadSettings.ts#L263-L266)); `savePreset` validates but the `switchConfig` webview message path does not. **[fixed]** — one `isValidPresetName` now gates `getPresetPath`, `savePreset`, and `loadPreset` (Pass 5.6).
-   Lock-file PID is type-checked but not range-checked ([baseServerManager.ts:437-445](../frontend/vscode-calcpad/src/baseServerManager.ts#L437-L445)); a poisoned `{"pid": -1}` reaches `process.kill(-1, 'SIGTERM')` on POSIX. **[fixed]** — `isValidPid` in the single shared `readLockFile`, ports range-checked alongside (Pass 5.6).
-   Child server inherits full `process.env` with no `ASPNETCORE_ENVIRONMENT` pin, and `appsettings.Development.json` ships in the VSIX — a developer with that variable exported gets Swagger and the `debug-crash` endpoint enabled. **[fixed]** — the file no longer ships in any bundle, and both hosts now pin `ASPNETCORE_ENVIRONMENT=Production` on the child's environment (Pass 5.6).
-   ~~Preview panels leave `localResourceRoots` at default (every workspace folder). They load no local resources — images are inlined as data URIs — so `localResourceRoots: []` is free.~~ **DONE in Pass 1 (2026-08-13)**, on the four preview panels, the `.cpdz` editor and its report.
-   CSP nonce in the Vue panel uses `Math.random()`, not a CSPRNG ([calcpadVueUIProvider.ts:753-760](../frontend/vscode-calcpad/src/calcpadVueUIProvider.ts#L753-L760)). **[fixed]** — `crypto.randomBytes(24).toString('base64url')` (Pass 5.6).
-   `--no-sandbox` on the PDF Chromium ([PdfGeneratorService.cs:302](../backend/Services/PdfGeneratorService.cs#L302), and again on the downloaded-Chromium fallback at [:325](../backend/Services/PdfGeneratorService.cs#L325)) runs attacker-controlled HTML unsandboxed. Acceptable for a loopback desktop tool; a blocker if server mode is ever finished. **[verified]**
-   No rate limiting anywhere. Fine locally, not fine in server mode. **[verified]**

## Non-findings

Recorded so they are not re-investigated:

-   `dotnet list package --vulnerable --include-transitive` is clean; `Microsoft.OpenApi` is pinned against GHSA-v5pm-xwqc-g5wc.
-   No hardcoded secrets across `.cs`/`.ts`/`.json`/`.rs`/`.sh`. The one thumbprint in `.env.example` is a commented-out code-signing cert SHA1 — a public identifier.
-   No `shell: true`, `eval`, or `new Function` in any extension-host code. All spawns use argv arrays.
-   `withGlobalTauri` and `dangerousRemoteDomainIpcAccess` are absent from all three Tauri configs. Core ACL grants are read-only; no `shell:allow-execute`/`allow-spawn`.
-   Both `unsafe` blocks in `lib.rs` (Win32 job-object FFI) are correct — null-handle checks, correct `size_of`, deliberate handle leak required by `KILL_ON_JOB_CLOSE`.
-   Cargo advisories `glib 0.18.5` (RUSTSEC-2024-0429) and `proc-macro-error` (unmaintained) are transitive through the Tauri v2 Linux stack and not fixable from this manifest. `ring 0.17.14` is **not** affected by RUSTSEC-2025-0009.
-   `capabilities.untrustedWorkspaces` is absent from the extension manifest. VS Code's default for an undeclared extension is *disabled in Restricted Mode* — fail-safe, but implicit. **[fixed]** — now declared explicitly as `supported: false` with a reason, so it survives future default changes (Pass 5.6).

## Remediation plan

Five passes, ordered by risk reduction per unit of work. Each is independently landable.

### Pass 1 — Contain webview script execution (Critical) — **DONE (2026-08-13)**

Landed in [previewFrame.ts](../frontend/vscode-calcpad/src/previewFrame.ts) (new), with [extension.ts](../frontend/vscode-calcpad/src/extension.ts) and [calcpadCompiledEditorProvider.ts](../frontend/vscode-calcpad/src/calcpadCompiledEditorProvider.ts) rewired to it.

1. **[x] CSP emitted — but deliberately with _no_ nonce.** The meta policy is on all three preview paths plus the loading, empty and error pages. **Do not add a nonce here.** A `srcdoc` document inherits its parent's policy, and per CSP3 a policy containing a nonce makes the browser ignore `'unsafe-inline'` — so nonce-ing the shell silently kills every inline author `<script>` in the frame (`Koch Snowflake Animated.cpd`, the Plotly examples). Nonce and author inline script are mutually exclusive in one document; only a frame loaded from a real URL rather than `srcdoc` could have both, which `localResourceRoots: []` rules out. [calcpadVueUIProvider.ts](../frontend/vscode-calcpad/src/calcpadVueUIProvider.ts) remains the right model for its own panel, which renders no author content.

    For the same reason `script-src` uses the bare `https:` scheme, matching calcpad-desktop, rather than a named CDN allowlist. An allowlist is unknowable by inspection: a bundle resolves its own dependencies at runtime from hosts absent from the worksheet — `dxf-viewer` from jsdelivr pulls `opentype.js` from `cdn.skypack.dev` — and a refused fetch does not throw, so each miss fails silently. `connect-src` needs `blob:` for the Blob → object-URL → `fetch` pattern the DXF module uses. Both are asserted in the verification script.

2. **[x]** Document body renders in a sandboxed `<iframe srcdoc>`, `allow-scripts` without `allow-same-origin`, mirroring [App.vue:415-423](../frontend/calcpad-web/src/App.vue#L415-L423). srcdoc escaping verified byte-for-byte against `parse5` with all five payloads from the finding present.
3. **[x]** `localResourceRoots: []` on the four preview panels, the `.cpdz` custom editor and its report panel.
4. **[x]** `sanitizeServerHtml` → `repairStrayAngleBrackets`, with both the function and call-site comments stating plainly that it is a rendering repair and not a security control.

Consequences of moving the document a frame down, also landed: injected scripts relay through `window.parent` instead of `acquireVsCodeApi`; `enableFindWidget` replaced by a shell-hosted find widget driving an in-frame marking walk; external links handed to `vscode.env.openExternal` (webview navigation interception does not reach a sandboxed frame); scroll offset and `#UI` focus/caret carried host-side per panel (VS Code restores scroll only for a webview's top-level document); and `previewDiagnosticsScript` in [calcpad-frontend](../frontend/calcpad-frontend/src/services/preview-diagnostics.ts), shared with calcpad-web, relaying CSP violations and resource load failures — neither of which any console relay can see.

**Not verified in a running editor:** the `#UI` form round-trip, the `.cpdz` editor and report, find, scroll restore and external links. DXF rendering and the right-click menu were confirmed by hand.

### Pass 2 — One-line, zero-breakage config fixes

1. ~~Delete `BrowserPath` from `PdfGenerateRequest` and drop the parameter from `GeneratePdfAsync`/`GenerateBasicPdfAsync`/`GetOrCreateBrowserAsync`/`ResolveBrowserPathAsync`. Keep the `appsettings`/`BROWSER_PATH` resolution. Update `API_SCHEMA.md`.~~ **Done** in commit `962875d` — the property, the parameter on all four methods, and the request-body path into `Puppeteer.LaunchAsync` are all gone; `API_SCHEMA.md` now documents that the browser is resolved from host configuration only.
2. ~~Delete `{"path": "**"}` from the three `fs` read scopes; add `fs:deny-default`. Replace the `/etc/os-release` read with a narrow `#[tauri::command]` or scope it to `{"path": "/etc/*-release"}`.~~ **Done** — `**` removed from all three read scopes, `fs:deny-default` added, and `fs:allow-read-text-file` scoped to `/etc/*-release` so `detectLinuxDistro` still works.

    Out-of-`$HOME` documents keep working through the fs plugin's *runtime* scope, which the dialog plugin extends per pick (`allow_file` in `tauri-plugin-dialog`'s `open`/`save`); `resolve_path` ORs that scope with the capability's. Three flows reach the plugin with no dialog, so each grants scope explicitly: `allow_document_dir` widens a picked document to its folder for `inlineImageSources` (refuses any path the runtime scope doesn't already allow, so script can't self-grant), the folder dialog passes `recursive: true`, and both argv paths in `lib.rs` call `allow_file_and_parent`. `fs:deny-default` overrides all of it — the fs plugin checks ACL denies independently of the runtime scope.

    `tauri-plugin-persisted-scope` 2.3.7 carries those runtime grants across restarts, so a recent file or restored opened folder outside `$HOME` still opens. Registered immediately after `tauri_plugin_fs::init()` — it reads that plugin's scope in its own `setup` and silently no-ops if fs isn't managed yet. No ACL entry needed: the plugin exposes no commands and ships no `permissions/` manifest. Required bumping `rust-version` to `1.77.2` in `Cargo.toml`, otherwise cargo resolves down to the v1-era `0.1.3`.

    Its state file is `$APPDATA/.persisted-scope`, inside a tree the webview *can* write (`fs:allow-write-file` → `$APPDATA/**`) — but the plugin calls `forbid_file` on its own path at init, and `resolve_path` honours the runtime scope's forbidden patterns, so script cannot rewrite it to grant itself whole-disk read on the next launch. Verified against the crate source, not assumed.

    Accepted tradeoff: the scope file now grows with every path the user ever picks, so the webview's read reach widens over time and partly re-opens what removing `**` closed. Bounded by user action rather than by policy. Pruning it (drop entries whose paths no longer exist, or cap by count) is worth doing before release.

    Note this does **not** close the arbitrary-read primitive — `#include` is resolved by the sidecar (`CalcpadService.CreateIncludeDelegate`), which the Tauri ACL does not govern at all. With `connect-src http://127.0.0.1:*` and no auth on the sidecar, webview script still reads any file by POSTing a worksheet that `#include`s it. See Pass 4.2 / Pass 5.1.

**This one needs more investigation**
3. **Rewritten 2026-08-13 — harden the shell and the preview separately.** The original advice (remove `dangerousDisableAssetCspModification`, drop `'unsafe-inline'`/`'unsafe-eval'` and bare `https:`) cannot be applied to the single global policy without breaking both Monaco and the preview. What follows is what each actually requires, then a staged plan.

    **Why one policy cannot serve both.** The preview is origin-isolated but **not CSP-isolated**: its frames are `srcdoc`, and a `srcdoc` document *inherits its parent's policy* (the Medium finding above). So `tauri.conf.json`'s CSP is simultaneously the app shell's policy, Monaco's policy, and every rendered worksheet's policy. Their requirements conflict:

    | Directive | Monaco (ESM, 0.52) | Rendered worksheet |
    | --- | --- | --- |
    | `script-src 'unsafe-inline'` | not needed | **required** — inline author `<script>` |
    | `script-src https:` | not needed | **required** — CDN imports |
    | `script-src 'unsafe-eval'` | probably not needed (ESM) | possibly, per library |
    | `style-src 'unsafe-inline'` | **required, unfixable** | required |
    | `worker-src` | `'self'` (bundled asset URL) | `blob:` + `https:` |
    | `connect-src` | `'self' ipc:` + loopback | `blob: data: https:` |

    Two entries deserve care:

    - **`style-src 'unsafe-inline'` is a hard Monaco requirement and cannot be nonced.** Monaco injects styles dynamically and uses `innerHTML` to set itself up, so a nonce cannot be attached by hooking `createElement("style")`, and hashes are impractical because the styles differ per load. Tracked upstream since [#271](https://github.com/microsoft/monaco-editor/issues/271) and still open as of June 2025 in [#4927](https://github.com/microsoft/monaco-editor/issues/4927). `Environment.createTrustedTypesPolicy` addresses Trusted Types, not CSP nonces. **Do not spend time trying to remove this** — record it and move on.
    - **`'unsafe-eval'` traces to the deprecated AMD `loader.js`** ([#110](https://github.com/microsoft/monaco-editor/issues/110), [#2488](https://github.com/microsoft/monaco-editor/issues/2488)). This app uses the ESM build through Vite, so it most likely does **not** need it. That makes it the one script-src tightening available today — but verify by removing it and exercising the editor rather than assuming.

    Bare `https:` in `script-src` stays for the preview: a named CDN allowlist is unknowable by inspection, since a bundle resolves its own dependencies at runtime from hosts absent from the worksheet (`dxf-viewer` from jsdelivr pulls `opentype.js` from `cdn.skypack.dev`), and a refused fetch does not throw so each miss fails silently.

    **Stage A — no architecture change.** Fixes silent breakage and takes the free wins. **Done** — the four directive changes below landed in `tauri.conf.json`; `'unsafe-eval'` was left in place and is still open.

    - **Three gaps are refusing things worksheets legitimately do.** `connect-src` has no `blob:`, so the Blob → `createObjectURL` → `fetch` pattern is refused — exactly what broke the DXF module under Pass 1's first policy, so it is very likely already broken on desktop. There is no `worker-src`, so it falls back to `default-src 'self' https:` and a `blob:` worker is refused. There is no `media-src`, so `data:`/`blob:` audio and video are refused. Target: `connect-src … blob: data:`, `worker-src 'self' blob: https:`, `media-src data: blob: https:`.
    - Add `object-src 'none'`, `base-uri 'none'`, `form-action 'none'` — free, and `form-action` removes the phishing-form half of the impact.
    - Try dropping `'unsafe-eval'` from `script-src`; keep `'unsafe-inline'`, bare `https:` and `dangerousDisableAssetCspModification`.
    - Confirm with the shared `previewDiagnosticsScript` relay (Pass 1, wired into calcpad-web): open `Examples/Isaiah/calcpad-dxf.cpd` on desktop and read the Preview Console. `[CSP] blocked blob:… - violated connect-src` confirms the gap before and its absence confirms the fix.

    **Accepted tradeoff recorded here because `tauri.conf.json` takes no comments.** The window's `additionalBrowserArgs` disables `IsolateSandboxedIframes`, so the sandboxed preview frames run *in* the shell's renderer process rather than their own. That boundary is a defence in depth on top of the opaque origin, not the primary one — but giving it up is a real loss, and it buys correct scrolling: WebView2 routes wheel events to an out-of-process sandboxed frame incorrectly, so scroll does nothing until the frame's native scrollbar is dragged. Enabled by default from [runtime 132](https://github.com/MicrosoftEdge/WebView2Announcements/issues/99). The other three features in that string (`msWebOOUI`, `msPdfOOUI`, `msSmartScreenProtection`) are wry's own defaults, carried over because setting `additionalBrowserArgs` replaces wry's string wholesale rather than appending to it. Revisit when Stage B lands: a preview served from a custom URI scheme is no longer a `sandbox`-without-`allow-same-origin` frame in the same way, so the feature may become re-enableable.

    **Stage B — split the policies, which is what actually hardens the shell.** Serve rendered documents from a **custom Tauri URI scheme** instead of `srcdoc`. `register_uri_scheme_protocol` hands back an `http::Response` whose headers you control, so the preview can carry its own permissive `Content-Security-Policy` header; a document fetched from a real URL does **not** inherit the embedder's policy, whereas `srcdoc`, `data:` and `blob:` all do — they are not an escape. Keep `sandbox="allow-scripts"` on the frame, so the preview gets a real URL for policy purposes *and* an opaque origin for isolation.

    With the preview no longer inheriting, the global policy can finally tighten to what the shell needs: `script-src 'self'` (drop `'unsafe-inline'`, `'unsafe-eval'` and bare `https:`), `connect-src 'self' ipc: http://127.0.0.1:*` (drop `https:`), `worker-src 'self'`, `img-src 'self' data: blob: asset: http://asset.localhost`. That removes the amplifier the High finding names — bare `https:` as both script origin and exfiltration sink — from the one document that holds IPC. `style-src` keeps `'unsafe-inline'` for Monaco, permanently. `dangerousDisableAssetCspModification` can then narrow from `["script-src", "style-src"]` to `["style-src"]`: once `script-src` no longer relies on `'unsafe-inline'`, Tauri's nonce injection for scripts becomes desirable rather than destructive.

    **Incidental, found while checking this.** [workers.ts](../frontend/calcpad-web/src/editor/workers.ts) returns `editor.worker.js` for every label, so the TypeScript, JSON, HTML and CSS workers named in [vite.config.ts](../frontend/calcpad-web/vite.config.ts) `optimizeDeps` are never instantiated. Dropping them trims the bundle and the surface. Monaco's worker is a bundled asset URL, not a blob, so the shell needs only `worker-src 'self'`.

    **VS Code parity.** The extension keeps `srcdoc` and needs no equivalent: its shell document is extension-authored only, holds no Monaco and no app, and `localResourceRoots: []` plus the closed message switch already bound it. The inherited-policy problem only bites where the embedder is itself a privileged application.
4. ~~Delete `shell:allow-open` from the capability, plus `tauri_plugin_shell::init()` and the `Cargo.toml` dependency if nothing else uses it.~~ **Done** — nothing used it; the app opens URLs/paths through the scoped `opener` permissions. Removed the capability grant, the `.plugin(tauri_plugin_shell::init())` registration, `tauri-plugin-shell` from `Cargo.toml`/`Cargo.lock`, and `@tauri-apps/plugin-shell` from `calcpad-web`'s `package.json`/lockfile.
5. ~~Add `bin/logs/**`, `.env.example`, and `bin/appsettings.Development.json` to `.vscodeignore` after the `!bin/**` line.~~ **Done** — but *not* after `!bin/**`: that negation had to be deleted, since vsce applies negations after all ignores and they would have won. Also fixed at the source (`CopyToPublishDirectory=Never` in the csproj) and in the Tauri bundles: the `binaries/*.json` resource glob in all three `tauri.*.conf.json` files was swept up by `appsettings.Development.json` and is now an explicit four-file list, and `build-portable.ps1` filters `appsettings.Development.json` and `*.log` out of the staged release directory (`stage-sidecar` mirrors the publish tree there and the server writes logs into it).

    **Still to do:** delete the local `vscode-calcpad/bin/logs/` — as of the 2026-08-13 verification it still holds ten server logs plus a 291 MB `last-crash.dmp`. No packaging risk remains (`.vscodeignore` excludes the tree and `bin/` is gitignored), so this is housekeeping. Separately, 0.3.0 / 0.4.0 / 0.4.2 still need republishing if they reached the Marketplace.

### Pass 3 — Supply chain

**Done.** All three items landed; the shared machinery lives in [downloadVerification.ts](../frontend/vscode-calcpad/src/downloadVerification.ts) — `assertAllowedUrl`, `verifySha512`, `fetchJsonFromAllowedHost`, `downloadVerified`, `extractZipToDir`. Verified live against both feeds: three `.nupkg` downloads and the 46 MB `win-x64` runtime archive all pass, and the four negative cases (off-allowlist host, `http:` scheme, tampered payload, malformed digest) all throw.

1. ~~Read the `hash` field from the .NET release entry and verify SHA512 before writing the archive; assert `new URL(file.url).host` against an allowlist (`builds.dotnet.microsoft.com`, `download.visualstudio.microsoft.com`).~~ **Done** — `resolveDownloadUrl` became `resolveDownload` and now returns `{ url, name, hash, version }`. The `hash` field is read from the chosen file entry, shape-checked as 128 hex chars, and a missing or malformed one is fatal (the user is pointed at the manual download page) rather than a silent skip. The archive is verified in memory by `downloadVerified` before `writeFileSync` runs, so unverified bytes never touch disk. The allowlist applies to the `releases.json` fetch as well, not just the archive URL.

    Also tightened the file lookup while in there: current release metadata omits the version from `name` (`aspnetcore-runtime-win-x64.zip`, not `aspnetcore-runtime-10.0.11-win-x64.zip`), so the old primary match never fired and every install was landing on the rid+extension fallback — which also matches `aspnetcore-composite-*` and `aspnetcore-targeting-pack-*` and was picking the right file only by array order. Both name shapes are now tried first, and the fallback requires an `aspnetcore-runtime-` prefix and excludes `composite`.

2. ~~Verify SHA512 per `.nupkg`, or move to the NuGet v3 flat-container endpoint with published hashes, or ship the DLLs in the VSIX and drop the runtime download entirely.~~ **Done** via the flat-container route. `https://www.nuget.org/api/v2/package/{id}/{version}` publishes no digest, so both `.nupkg` paths (managed deps and native `libSkiaSharp`) now resolve through the v3 registration leaf `api.nuget.org/v3/registration5-semver1/{id}/{version}.json`, which yields the flat-container `packageContent` URL plus a `catalogEntry`; the catalog entry carries `packageHash` (base64) and `packageHashAlgorithm`. Anything other than `SHA512` is rejected. Both download sites share one `downloadVerifiedNupkg` helper, so the native lib is hash-checked before it is written and `chmod 0o755`'d.

3. ~~Switch the Windows runtime extraction to `ZipFile::ExtractToDirectory` (already used elsewhere in the same file) so entry paths are validated.~~ **Done** — the extractor moved out of `CalcpadServerManager` into the shared module and the runtime `.zip` branch now calls it, so all three archive sites share one validated code path. Its env-var argument passing was kept (renamed to `CALCPAD_ZIP_SRC`/`CALCPAD_ZIP_DST`) so extension paths containing quotes or spaces still can't break shell quoting.

*This can be addressed in the future, unlikely scenario:*

Not addressed, and worth deciding before release: both feeds are their own trust root — a compromised `releases.json` or NuGet catalog could publish a hash matching a malicious payload. Closing that needs digests pinned in-repo (or Authenticode/NuGet signature validation), not fetched. What this pass does close is TLS-stripping, feed-URL redirection to an off-allowlist host, tampering at the CDN edge, and truncated or corrupt downloads.

*This can be addressed when doing the non-localhost server build*
### Pass 4 — Backend hardening

1. Cap `.cpdz` inflation with a counting stream that aborts past a fixed ceiling; set an explicit `MaxRequestBodySize` on the decode endpoint.
2. Decide whether `/portable/package`'s raw-bytes read stays in the accepted threat model. If not, confine resolution to the source file's directory tree plus the declared `#ProjectPath`/`#LibraryPath` roots.
3. Apply the same containment to [calcpadLocationResolver.ts:59-68](../frontend/vscode-calcpad/src/calcpadLocationResolver.ts#L59-L68), and stop expanding environment variables from untrusted document text.


### Pass 5 — Authentication and residual hardening

**Done.** All six items landed.

1. ~~Generate a random per-launch token, pass it to the sidecar via environment (**not** argv — argv is world-readable in `/proc`), require it as a header on every `/api/**` route, and hand it to the webview via `server_url` or a dedicated command. Create the port file mode `0600`.~~ **Done.** `CALCPAD_API_TOKEN` is read once at startup ([CalcpadApiService.cs](../backend/Services/CalcpadApiService.cs)); when present, a middleware registered right after `UseCors` rejects any `/api` request whose `X-Calcpad-Token` header does not match, compared with `CryptographicOperations.FixedTimeEquals`. `OPTIONS` is skipped and the middleware sits behind the CORS middleware, so a preflight — which carries no custom headers — is answered before the check and the real request is never blocked ahead of being sent.

    Both hosts generate 32 bytes from the OS CSPRNG and pass it in the child's environment block. Tauri: `api_token()` in [lib.rs](../frontend/calcpad-desktop/src-tauri/src/lib.rs) (`getrandom`, hex), held in a `OnceLock` so a sidecar restart keeps the token the webview already has, exposed to the webview as the `server_token` command and fetched by `TauriServerManager.start()`. VS Code: `crypto.randomBytes(32)` in [baseServerManager.ts](../frontend/vscode-calcpad/src/baseServerManager.ts), published in the lock file — now written mode `0600` — so peer windows sharing the server adopt it, with `getAuthToken()` feeding `apiClient.setAuthToken()` at all three server-start/restart sites.

    The client sends it everywhere: `CalcpadApiClient` adds the header to all its own requests, and `authHeaders()` covers the handful of direct `fetch` sites (both PDF bridges, the two `pdf-browser` helpers, three sites in `extension.ts`). It is **withheld for a non-loopback base URL** — `setBaseUrl` can be pointed at a configured remote server by a preset carrying `server.url`, and that host has no business seeing a local secret.

    Verified live: no token → 401, wrong token → 401, a prefix of the token → 401, correct token → 200, on both a GET and a `[FromBody]` POST; preflight → 204 with `Access-Control-Allow-Headers: content-type,x-calcpad-token`; and with the variable unset the server serves as before and logs a warning naming the exposure. The port file is created empty, chmod'd, then written, so the URL is never briefly world-readable in `/tmp`.

    Correction (2026-08-13): that preflight response is real, but it is **not** produced by a scoped header allowlist — the policy is `AllowAnyHeader()` ([CalcpadApiService.cs:50](../backend/Services/CalcpadApiService.cs#L50)), and ASP.NET Core echoes back whatever `Access-Control-Request-Headers` asked for. Nothing here is exploitable on its own (`AllowCredentials` is absent and `SetIsOriginAllowed` still gates the origin), but narrowing it to `WithHeaders("content-type", "x-calcpad-token")` is a free tightening and would make the observed response mean what it appears to mean.

    Not closed by this: a launch with no `CALCPAD_API_TOKEN` — `dotnet run`, the sample client, the test harness — is still unauthenticated by design. That is the development path, and it is now loud rather than silent.

    Also not covered: the middleware keys on `Request.Path.StartsWithSegments("/api")`, and `UseSwagger()`/`UseSwaggerUI()` ([CalcpadApiService.cs:230-234](../backend/Services/CalcpadApiService.cs#L230-L234)) are mapped outside that prefix, gated only by `IsDevelopment()`. Every route that does real work is on `CalcpadController` under `api/[controller]`, and no static files, hubs, websockets or fallback routes are mapped at all, so Swagger is the sole uncovered surface. Inert in practice — both shipped hosts pin `ASPNETCORE_ENVIRONMENT=Production` — but recorded so it is not rediscovered as a finding.

2. ~~Validate `path` inside `open_path_native` against the opener allow-list; reject leading `-`; pass `--` before the path.~~ **Done**, with one deliberate deviation: `xdg-open` has no `--` end-of-options marker — it is a shell script that would try to open a file literally named `--` — so passing one would break every open on Linux. Containment is the control instead: `is_openable` canonicalizes the path and requires it under `app_data_dir`, `app_config_dir`, or `app_log_dir`, mirroring the capability scope below, and canonicalizing on both sides so `..` and symlinks cannot walk out. The leading-`-` check is kept as a second line.

3. ~~Narrow `opener:allow-open-path` to `$APPDATA/**` and `$APPLOG/**`; drop `$RESOURCE/**`; prefer `reveal_item_in_dir` over `open_path` for user documents.~~ **Done** — the scope is now `$APPDATA/**`, `$APPCONFIG/**`, `$APPLOG/**`; `$HOME/**` and `$RESOURCE/**` are gone, which breaks the write-then-execute chain (the webview can still write under `$HOME`, but can no longer hand what it wrote to `ShellExecute`). The four remaining open-path callers — settings folder, fonts folder, logs folder, and the log dir's `$RESOURCE` fallback — all sit inside the narrowed scope once that dead fallback is dropped. The one caller that genuinely takes an arbitrary user path, "Open PDF" after an export, now calls `revealItemInDir`; the button reads **Show in Folder**. `reveal_item_in_dir` takes no scope (confirmed against the plugin source — it is the one opener command with no scope check) but only selects the file in the file manager and never executes it, which is the point.

    **Regression caught 2026-08-13, fixed.** A *second* caller took an arbitrary user path and was missed: `handleOpenContainingFolder` ([tauri-bridge.ts](../frontend/calcpad-web/src/services/tauri-bridge.ts)) took the parent of a workspace file and handed it to `openPathSafe`, so the **Open Containing Folder** action — reachable from both the file-tree context menu and the tab context menu — was rejected on every platform after the narrowing: by the capability scope on Windows/macOS, by `is_openable` on Linux. The `catch` around it swallowed the error, so it silently did nothing rather than reporting a failure. Now `revealItemInDir(itemPath)`, matching the "Open PDF" fix, which also selects the file rather than merely opening its folder.

    **Amended 2026-08-23 — narrowing reversed for `open_path`.** `opener:allow-open-path` is now `allow: ["**"]` with `deny: ["$RESOURCE/**"]`, and the "Open PDF" prompt opens the file again rather than revealing it. Three things drove this:

    - **The threat model needed a webview compromise, and there is no route to one.** Worksheet script runs only inside `sandbox="allow-scripts"` iframes with no `allow-same-origin` ([App.vue:415-423](../frontend/calcpad-web/src/App.vue#L415-L423)), so it sits at an opaque origin with no `window.parent`, no popups, and no reach to the bundled `invoke`. No sink puts worksheet-derived content into the main webview as HTML — every display path is a Vue `{{ }}` or attribute binding, and the only two `v-html` uses are hardcoded SVG icons. Reaching `ShellExecute` needs a genuine XSS in the Vue app, Monaco, or a WebView2/WebKitGTK escape.
    - **A hostile `.cpd` is not the right yardstick.** One already reads on-disk images through the host and inlines them into a frame with `https:` egress. The line the narrowing defended was RCE-over-exfil, which is a real difference of degree but does not justify the cost below.
    - **It made desktop stricter than the surface shipped beside it.** The VS Code extension has always done `vscode.env.openExternal(saveUri)` on an arbitrary saved path with no gate ([extension.ts](../frontend/vscode-calcpad/src/extension.ts)). Two hosts, one product, opposite postures — and the desktop half was the one that lost a working feature.

    `$RESOURCE/**` stays denied, but **as a lifecycle guard, not a security boundary** — do not read it as one. What it prevents is real: `$RESOURCE` holds the sidecar apphost, the token exists only on the child's env block ([lib.rs](../frontend/calcpad-desktop/src-tauri/src/lib.rs)) rather than on this process, so a `Calcpad.Server` started through `open_path` would come up with no `CALCPAD_API_TOKEN` and no pinned `ASPNETCORE_ENVIRONMENT` — an unauthenticated server with Swagger and the debug-crash endpoint live, arbitrary file read via `#include`, on an unknown port, orphaned from `stop_sidecar`. But `allow: ["**"]` strictly dominates it: anything that can call `open_path` can equally call it on `cmd.exe` or any other binary on the machine. Blocking one executable while permitting every other one buys nothing against a compromised webview. It is kept because it costs one line and breaks nothing — the only remaining `$RESOURCE` consumer is `server_dir`, which reads log tails rather than opening anything — and it stops our own code, or some future feature taking a path through here, from launching the bundled sidecar out of band. `deny` takes precedence over `allow` in `tauri::fs::Scope`, which is what the opener plugin evaluates its scope with.

    `is_openable`/`openable_roots` are deleted — they mirrored a scope that no longer exists, and Linux would have stayed broken otherwise since `open_path_native` bypasses the ACL by design. The leading-`-` rejection moved into `open_path_native` itself.

    **Two weaknesses this leans on, worth fixing independently of PDF export:** the CSP is `script-src 'self' 'unsafe-inline' 'unsafe-eval' https:` ([tauri.conf.json](../frontend/calcpad-desktop/src-tauri/tauri.conf.json)), so it would not contain an injection if one appeared — and `https:` would let injected script fetch a remote payload; and `app.security.pattern` is unset (brownfield), so any main-webview script reaches `invoke` with no second gate. The absence of a sink is a property of today's code, not an enforced invariant.

4. ~~Restrict `get_env` to an allow-list (`HOME`, `USERPROFILE`, `APPDATA`, `XDG_*`, `CALCPAD_*`).~~ **Done** — exactly that list, with `CALCPAD_API_TOKEN` carved back out. It should never be in this process's environment (it is set on the child's env block only), but `expandEnvVars` feeds this command names lifted straight out of worksheet text, and the preview frame that would render the result has network egress.

5. ~~Resolve the updater half-state — remove the plugin, capability, and dependency, or configure a real `pubkey` and HTTPS endpoints.~~ **Done** by removal — there is no update endpoint to point at. Dropped `tauri_plugin_updater::Builder` from `lib.rs`, `updater:default` from the capability, the dead v1-shaped `plugins.updater` block from `tauri.conf.json`, `tauri-plugin-updater` from `Cargo.toml`, and `@tauri-apps/plugin-updater` from `calcpad-web`'s `package.json`. `Cargo.lock` shed 425 lines.

6. ~~Clear the Low table.~~ **Done.** Preset names now go through one `isValidPresetName` used by `getPresetPath`, `savePreset`, and `loadPreset`, so the `switchConfig` webview path can no longer traverse. Lock-file PIDs are range-checked (`isValidPid`: positive integer, ≤ `0xffffffff`) in the one `readLockFile` both call sites now share, so `{"pid": -1}` can never reach `process.kill(-1, 'SIGTERM')`; ports are range-checked alongside. `ASPNETCORE_ENVIRONMENT=Production` is pinned on the child in both hosts. The Vue panel's CSP nonce is `crypto.randomBytes(24).toString('base64url')`. `capabilities.untrustedWorkspaces.supported = false` is declared explicitly in the extension manifest with a reason, rather than relying on VS Code's implicit default.

    Two Low items are deliberately still open: `--no-sandbox` on the PDF Chromium and the absence of rate limiting. Both are recorded above as acceptable for a loopback desktop tool and blockers only for server mode.

## What holds up

Worth preserving through any refactor — several of these are why the Critical finding stays contained.

-   **Loopback is enforced twice, on intent and on outcome.** A pre-bind check on the intended URL ([Program.cs:192-199](../backend/Program.cs#L192-L199)) and a post-bind re-check against `app.Urls` ground truth ([Program.cs:236-249](../backend/Program.cs#L236-L249)) that specifically catches a `Kestrel:Endpoints` override slipping past `UseUrls`. Both hard-fail.
-   **The threat model is written down.** [CalcpadApiService.cs:58-113](../backend/Services/CalcpadApiService.cs#L58-L113) names the exact attack, states plainly that the random port is not a defense and why, explains that `[FromBody]` + `[ApiController]` forces a preflight, and rejects `"null"` origin because that is what the app's own sandboxed frames send. The `Host`-header check at [:146-159](../backend/Services/CalcpadApiService.cs#L146-L159) closes DNS rebinding, which is the piece most projects miss.
-   **The desktop untrusted-content boundary is correct.** `sandbox="allow-scripts"` deliberately without `allow-same-origin`, with [App.vue:379-382](../frontend/calcpad-web/src/App.vue#L379-L382) explaining why adding it would hand worksheet script the Tauri IPC. The postMessage handler validates by window identity rather than origin — correct, since every opaque frame reports `origin === "null"`.
-   **Draft path handling.** `validate_draft_id` rejects anything outside `[A-Za-z0-9_-]`, and is re-applied on the *listing* path to filter ids that reached disk some other way.
-   **Extension settings live in `globalStorage`, not workspace configuration** ([calcpadSettings.ts:258-271](../frontend/vscode-calcpad/src/calcpadSettings.ts#L258-L271)), so a malicious repo's `.vscode/settings.json` cannot set `dotnetPath` or the server URL. This closes the single most common settings-to-RCE path in VS Code extensions.
-   **No `shell: true` anywhere; all spawns use argv arrays.** Windows zip extraction passes paths via environment variables specifically to dodge shell quoting, with a comment saying so.
-   **`debug-crash` is gated on `IsDevelopment()`** ([CalcpadController.cs:210-216](../backend/Controllers/CalcpadController.cs#L210-L216)), with a comment noting that a preflight-free GET would otherwise let any visited page kill the local server.
-   **Sidecar lifecycle is leak-free** — Windows job object with `KILL_ON_JOB_CLOSE`, `--parent-pid` polling, stdin-EOF watchdog, and a generation counter that prevents a dying old sidecar from clobbering a newly spawned one.
